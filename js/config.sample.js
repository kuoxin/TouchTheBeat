define([], function () {
    return {
        /**
         * specify the backend host here. https should be used in production
         */
        host: 'http://localhost/ttb-backend/',

        /**
         * when this option is true the app object is accessible in the console at window.app
         */
        debug: true
    };
});
